# MSMD Blender Files

Kaupapa: Moko See Moko Do (MSMD) — Blender assets, scripts, and notes.
- Tracks .blend via Git LFS
- Renders/caches ignored
- Daily flow: `git add -A` ? `git commit -m "msg"` ? `git push`

Folders (suggested):
01_src/    # SVGs, refs, scripts
02_assets/ # textures PNG, HDRI (LFS if large)
03_blend/  # .blend (LFS)
04_renders/# OUT of git (ignored)
